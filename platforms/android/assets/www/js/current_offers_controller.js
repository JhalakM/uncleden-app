angular.module('starter.current_offers', [])

.controller('current_offersController', function($scope, $http, $state,  $ionicLoading, $timeout, $ionicPlatform) {
    $ionicPlatform.ready(function() {

    });
});