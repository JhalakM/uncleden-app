angular.module('starter.dashboard', [])

.controller('dashboardController', function($scope, $http, $state,  $ionicLoading, $timeout, $ionicPlatform) {
    $ionicPlatform.ready(function() {

    });
});